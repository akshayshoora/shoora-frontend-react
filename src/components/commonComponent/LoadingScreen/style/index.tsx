import { makeStyles } from "@mui/styles";

export default makeStyles({
  root: {
    display: "flex",
    justifyContent: "center",
    alignItems: "center",
    height: "20vh",
    flexDirection: "column",
  },
});
