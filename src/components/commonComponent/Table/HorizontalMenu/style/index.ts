import { makeStyles } from "@mui/styles";

export default makeStyles({
    hrMenuOptionContainer: {
        display: "flex",
        marginBottom: 0,
        listStyleType: "none",
        paddingLeft: 0,
        fontSize: "14px",
        marginTop: "0px",
        "& li": {
            marginRight: "2px"
        }
    }
});
