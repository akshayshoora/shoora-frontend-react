import { makeStyles } from '@mui/styles';

export default makeStyles({
    disabledImage:{
        opacity:1,
        maxWidth:'25px',
        background:'white',
        borderRadius:'10px',
        padding:'7px',
    }
});
