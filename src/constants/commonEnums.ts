export enum AppPaths {
  DASHBOARD = 'dashboard',
  USERS = 'users',
  LOGIN = 'login',
  PROFILE = 'profile',
  ORGANIZATIONS='organizations',
  DEVICES='devices',
  DRIVERS='drivers',
  VEHICLES='vehicles',
  LIVE='live'
}

export enum SubPaths {
  ADD = 'add',
  EDIT = 'edit',
}

export enum Actions {
 ADD="POST",
 EDIT="PATCH",
 DELETE="DELETE",
 GET="GET",
}
